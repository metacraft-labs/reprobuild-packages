repos:
  - repo: local
    hooks:
      - id: repro-lint
        name: Package catalog graph
        entry: repro lint --tool-provisioning=path --daemon=off
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit, pre-push]
